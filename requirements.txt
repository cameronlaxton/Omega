# Compatibility install path for tools that do not read pyproject.toml directly.
# Preferred local dev install remains:
#   python -m pip install -e .[dev]
-e .
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.8.0
mypy>=1.13.0
# Integration-adapter deps (mirrors the `integrations` extra). pandas drives the
# tests/integrations dataframe transforms; pyarrow backs the `.parquet` ETL cache.
pandas>=2.2.2
pyarrow>=16.0
