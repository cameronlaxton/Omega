# Compatibility install path for tools that do not read pyproject.toml directly.
# Preferred local dev install remains:
#   python -m pip install -e .[dev]
-e .
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.8.0
mypy>=1.13.0
