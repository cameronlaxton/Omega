__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.vs/
.vscode/
.idea/
*.swp
*.swo

# Testing
.pytest_cache/
.coverage
htmlcov/

# Type checking
.mypy_cache/

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db
.fuse_hidden*

# Cache
.omega_cache/

# Node (frontend)
node_modules/
.next/
out/

# Logs
logs/
*.log

# Retired scratch audit artifacts — superseded by inbox/sessions/<id>.json
# (audit_events) + reports/run_audits/<id>.audit.md. Root-anchored so the
# committed copies under archive/legacy/ are unaffected; these must never
# reappear at the repo root. Do not regenerate.
/RUN_AUDIT.md
/RUN_TRACE.jsonl

# Phase 7 ETL raw-response cache (knowable-at-the-time snapshot; not source).
# Alias tables (data/aliases) and Sackmann CSV clones (data/external) ARE versioned.
data/cache/

# Large re-downloadable raw dataset dumps. The filtered/tournament CSVs derived
# from them (data/historical/raw/**/*.csv without the _raw suffix) ARE versioned
# as the reproducible replay inputs; the full multi-MB source dumps are not.
data/historical/raw/**/*_raw.csv

# Local DB / SQLite + FUSE probe artifacts (see OMEGA_COWORK.md §2b "Disk artifacts").
# Harmless leftovers from ad-hoc DB probing and deleted-but-open handles.
*.probe.db
*.probe.db-journal
*.db-wal-test
*.db-wal
*.db-shm
tmp*.probe.db*

# Quarantined malformed session sidecars + diagnostic JSONL event mirror.
inbox/sessions/invalid/
inbox/sessions/*.events.jsonl

# Runtime reports and processed traces (not source truth)
reports/*.txt
reports/run_audits/*.audit.md
inbox/traces/processed/

# Runtime state root (DB, inbox dumps, generated reports) must stay out of git.
var/
/inbox/
/reports/
/omega_traces.db
/omega_traces.db-shm
/omega_traces.db-wal
/omega_odds_api_budget.json

# Generated dashboard snapshot (derived artifact, regenerable — not source truth)
/omega-snapshot.html

# Champion/challenger scratch output (not versioned source data)
data/cc_phase3d_out.json

# Scratch scripts used by LLM for session processes
scratch/

# Ad hoc recommendation-table dumps written by scratch session scripts directly
# to the repo root instead of scratch/ or var/. Root-anchored; do not regenerate.
/scratch_recs.json
